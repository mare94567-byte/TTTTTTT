name: samaq_app
description: A new Flutter project for Manhwa.
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  image_picker: ^1.0.4  # لرفع الصور من الاستوديو
  shared_preferences: ^2.2.2 # لحفظ سجل المشاهدة والتحميلات

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
